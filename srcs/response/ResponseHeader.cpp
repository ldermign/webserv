#include "ResponseHeader.hpp"

std::vector<std::string>		ResponseHeader::week_days = init_week_days();
std::vector<std::string>		ResponseHeader::months = init_months();
std::vector<int>				ResponseHeader::redirection_status = init_redirection_status();
